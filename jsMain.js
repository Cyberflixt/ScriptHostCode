
var txtScript;

$(document).ready(new function() {
    txtScript = document.getElementById("txtScript");
    setTimeout(() => {
        txtScript.innerHTML = "No u."
    },5)
    //txtScript.innerHTML = "No u."
});